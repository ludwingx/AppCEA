import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ludwing.appcea',
  appName: 'CEA',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
